# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Updated for Java 17 compatibility
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=768m --enable-native-access=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-opens=java.base/java.lang.reflect=ALL-UNNAMED --add-opens=java.base/java.text=ALL-UNNAMED --add-opens=java.desktop/java.awt.font=ALL-UNNAMED

# Enable Gradle configuration cache
org.gradle.configuration-cache=true

# Enable parallel builds
org.gradle.parallel=true

# Use newer Gradle daemon
org.gradle.daemon=true

# Force newer Kotlin version compatibility
kotlin.code.style=official

# Suppress warnings about newer Java versions
org.gradle.warning.mode=none

# Force Java 17 for all modules
org.gradle.java.installations.auto-detect=false
org.gradle.java.installations.auto-download=false

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
